# What the latest results reveal

The latest run has moved the work beyond a catalog of holonomy angles. It now exposes a coherent physical mechanism:

**Precession is the conversion of ordered displacement into conserved orientation.**

In CGM this is not merely how an observer describes motion. It is what physically happens when individuality produces noncommuting displacement while identity requires the displaced configuration to remain traceable to its origin.

The endpoint closes under BU, so the residual cannot remain an ordinary displacement. It returns as a rotation in the stabilizer of the endpoint. That rotation is the preserved ancestry of the path.

This is why precession is universal in CGM. It is not one effect alongside gravity, spin, and angular momentum. It is the primitive mechanism through which those phenomena preserve directional information under motion.

The complete Palge-Pfeifer result is important here. It establishes that the canonical result is simultaneously:

- Einstein gyrogroup gyration
- Hyperbolic triangle defect
- Geodesic mass-shell holonomy
- Lorentz-factorization rotation
- Spin-connection holonomy

Thomas-Wigner rotation is physically the holonomy of the curved relativistic mass shell, and a spinor carried around that loop acquires the corresponding SU(2) transformation. ([arxiv.org](https://arxiv.org/abs/2310.08121))

## 1. The first major discovery: precession realizes three-dimensionality

The three elementary stage pairings are:

| Coupling | Angle | Emergent axis |
|---|---:|---|
| UNA with ONA | 0.396601502221 | z |
| ONA with BU | 0.097671089129 | x |
| BU with UNA | 0.083413894169 | y |

These are not three angles inserted into an already existing coordinate frame. The complementary axes are generated by the ordered pairings themselves:

- UNA and ONA generate the missing BU-oriented axis.
- ONA and BU generate the missing UNA-oriented axis.
- BU and UNA generate the missing ONA-oriented axis.

This is an operational realization of three-dimensional space.

Two independent operations do not merely occupy two directions. Their noncommutative composition produces the third rotational direction needed to remember their ordering. Repeating this for all three pairs closes an orthogonal frame.

That sharpens the 3D result considerably:

> **Three-dimensional space is the minimal self-completing precession system in which every pair of independent motions produces orientation around the complementary direction.**

This is a phenomenon, not a coordinate convention. The axes appear as outputs of gyration. A coordinate system is subsequently chosen to label them x, y, and z.

The precession analysis therefore supplies a mechanical realization of the BCH result. The earlier BCH proof says three generators are forced. The present result shows what those generators physically do: they are the three mutually completing precession channels.

## 2. The second discovery: the three motion types form a coupling triad

The CGM progression identifies three physical forms of motion:

- UNA: rotational variation
- ONA: translational displacement
- BU: bounded balance vibration

The precession triad gives all three pairwise couplings:

| Pair | Physical coupling |
|---|---|
| UNA–ONA | rotation with translation |
| ONA–BU | translation with balance |
| BU–UNA | balance with rotation |

This supplies a deeper interpretation of the three elementary angles.

### UNA–ONA: directional transport

The largest elementary angle,

```text
omega_UO = 0.396601502221
```

is the rotation-translation coupling. It controls how a balanced rotation axis is redirected when the surrounding path includes UNA and ONA.

Its most direct physical expression is axis steering. It is the channel through which translational motion carries rotational orientation.

This is the ancestral content of angular momentum. Directional information introduced by rotation is not lost during displacement. Instead, it is transported as a change in the orientation of the precession axis.

### ONA–BU: gravitational balance under displacement

The angle

```text
omega_OB = 0.097671089129
```

is the translation-balance coupling.

Its dual-pole completion gives

```text
delta_BU = 2 omega_OB
         = 0.195342178258.
```

This is precisely the channel already selected by the gravity program. Physically, that selection now has a clearer reason:

> Gravity is the balanced rotational memory generated when translation is carried through the dual poles of closure.

ONA supplies displacement. BU supplies the restoring relation to the common origin. Their interaction produces curvature.

The dual-pole factor of two is therefore not an arbitrary doubling. It is egress plus ingress, the outward and reconstructive sides of gravitational balance.

### BU–UNA: rotational balance

The third angle,

```text
omega_UB = 0.083413894169,
```

is the rotation-balance coupling. Its dual-pole completion is

```text
delta_UNA_BU = 2 omega_UB
             = 0.166827788338.
```

This appears to be the intrinsic rotational stabilization channel. It couples an oriented internal frame to the balance motion that preserves that orientation through closure.

Within the present mechanics, this is the strongest candidate for the channel underlying intrinsic spin, rotational inertia, or gravitomagnetic response. That identification is not yet derived, but the physical role is no longer unknown: it is the elementary exchange between rotational structure and balance.

The triad can therefore be read provisionally as:

```text
UNA–ONA: transport of orientation
ONA–BU: curvature and gravitational binding
BU–UNA: stabilization of intrinsic rotation
```

These are not three unrelated sectors. They are all pairwise exchanges among the three forms of motion forced by CS, UNA, ONA, and BU.

## 3. The third discovery: exact logical closure produces physical curvature

The stage thresholds satisfy the exact flat identity

```text
theta_cs + theta_una + theta_ona = pi.
```

The constitutional stage triangle has zero Euclidean defect.

Yet, after those thresholds are realized as displacements in relativistic velocity space, the corresponding loops have nonzero defects:

```text
omega_UO = 0.396601502221
omega_OB = 0.097671089129
omega_UB = 0.083413894169.
```

This reveals a fundamental distinction:

- The constitutional conditions close exactly.
- Their physical composition law is curved.
- The difference appears as precession.

This may be the cleanest statement of emergent curvature in the CGM program:

> **Curvature does not arise because the foundational requirements fail to close. It arises because exact logical closure is implemented by noncommuting physical displacement.**

The conditions are consistent at the constitutional level. Their realization has memory because the geometry of composition is non-Euclidean.

Precession is the observable conversion of exact operational closure into geometric path memory.

That directly joins the logical and physical layers:

```text
modal contingency
→ noncommutative composition
→ curved transport
→ holonomy
→ precession
```

## 4. The fourth discovery: identity is an angle, individuality is an axis

The palindrome satisfies

```text
R_pal = A^-1 R_BU A,
```

with

```text
A = gyr(UNA, ONA).
```

Conjugation preserves the rotation angle but changes the rotation axis.

The results are exact:

```text
theta_pal = theta_BU = delta_BU
axis turn = omega_UO
```

This gives a concrete physical decomposition of identity and individuality.

### Identity

The conjugacy class, represented by the angle `delta_BU`, survives the surrounding path. It is the invariant amount of balance memory.

### Individuality

The axis records how that invariant memory is oriented in the specific path. It changes under the outer UNA–ONA traversal.

Thus:

> **Identity is preserved in the magnitude of holonomy. Individuality is expressed in its orientation.**

This is not merely an analogy. It is the exact mathematical content of conjugation in the latest run.

The palindrome does not duplicate the BU loop. It embeds the same balance identity in a newly oriented physical context.

This is also the mechanical meaning of BU-Ingress. Reconstruction need not return every direction to its initial coordinate expression. It must recover the invariant relational content. The angle is reconstructed exactly while the axis retains the history of how the loop was embedded.

## 5. Precession is the physical memory carrier of CGM

The latest analysis supports the following hierarchy:

```text
endpoint memory        = whether the path closes
magnitude memory       = conjugacy angle
orientation memory     = rotation axis
ordering memory        = noncommutative word
representation memory = how the same group element acts on spin, vectors, fields, orbits
```

A scalar defect keeps only magnitude memory. A full SO(3) matrix retains angle and axis. An SU(2) lift also retains spinorial phase. A full Lorentz transformation retains rotational and boost memory.

This explains why the same basic mechanics can appear as:

- Thomas-Wigner rotation of a carried frame
- Spinor geometric phase
- Gyroscope precession
- Orbital precession
- Gauge holonomy
- Regge curvature
- Gravitomagnetic transport

These are not merely similar measurements. They are different representations of path memory.

Fermi-Walker transport gives the physical standard of nonrotation for an accelerated observer and is physically realized by gyroscopes. Thomas precession is the difference between that transported standard and the sequence of instantaneous rest frames. ([arxiv.org](https://arxiv.org/pdf/0804.2502))

## 6. A direct quantum-physical meaning already exists

The precession angles do not require a cycle period before they become physical.

A period is required to turn an angle into a frequency, but a closed-loop angle is already observable as a geometric phase.

An SO(3) holonomy with angle `theta` lifts to the spin-1/2 transformation

```text
U(theta, n) = exp(-i theta n·sigma / 2).
```

Its two spin eigenstates acquire phases

```text
exp(-i theta/2)
exp(+i theta/2).
```

The relative phase is `theta`.

Therefore the BU loop physically produces:

```text
branch phase magnitude = delta_BU / 2
relative spin phase    = delta_BU.
```

In action units:

```text
branch geometric action = hbar delta_BU / 2
relative action         = hbar delta_BU.
```

For the three elementary channels:

```text
S_UO,relative = hbar omega_UO
S_OB,relative = hbar omega_OB
S_UB,relative = hbar omega_UB.
```

This is analogous to the role of Planck’s constant raised by the user. Planck’s constant is both a conversion measure and the physical quantum of action. Similarly, the CGM holonomy angle is both a measurement and the actual geometric phase produced by transport.

The physical effect may be read through polarization, interference, frame orientation, or orbital direction depending on the representation. Wigner rotation has a direct action on quantum spin states and can entangle or correlate spin with momentum. ([link.aps.org](https://link.aps.org/doi/10.1103/PhysRevA.106.062404))

A cold-neutron experiment has even been proposed specifically to measure Wigner rotation as a cumulative spin effect. ([arxiv.org](https://arxiv.org/pdf/1604.03389v2.pdf))

So the universal physical statement is not merely “the angle per cycle exists.” It is:

> **Every CGM loop physically transports orientation and produces a representation-dependent geometric action.**

## 7. The aperture is now an equation of state, not only one ratio

The closure-response section is more important than its current placement suggests.

The function

```text
delta_BU(theta_ona, m_a)
```

is a constitutive law for converting balance amplitude into path memory.

The run now exposes three regimes.

### Infinitesimal closure

```text
rho0 = d delta_BU / d m_a at m_a = 0
     = 0.970231725282.
```

This is the initial linear conversion efficiency.

Its complement is

```text
baseline_gap = 1 - rho0
             = 0.029768274718.
```

Before finite BU amplitude contributes, ONA geometry alone leaves a 2.9768 percent opening.

### Integrated closure

```text
rho_secant = delta_BU / m_a
           = 0.979300454497.
```

This is the total conversion efficiency from zero amplitude to the physical operating point.

The ordinary aperture is

```text
Delta_secant = 0.020699545503.
```

Finite BU amplitude has closed

```text
finite_BU_closure = rho_secant - rho0
                  = 0.009068729215
```

of the initial opening.

### Incremental closure

```text
rho_tangent = d delta_BU / d m_a at physical m_a
            = 0.997796177590.
```

A small additional change around the physical operating point is converted into holonomy with 99.7796 percent efficiency.

The local residual is

```text
Delta_tangent = 1 - rho_tangent
              = 0.002203822410.
```

The response curve is convex:

```text
d2 delta_BU / d m_a^2 = 0.285536795041 > 0.
```

This means finite balance amplitude increases the efficiency with which further balance displacement becomes path memory.

The system becomes more responsive as it approaches its physical operating point.

## 8. A new exact closure budget

The reported numbers imply another exact decomposition that should be added to the gates:

```text
Delta_secant
= Delta_tangent + nonlinear_closure_gain.
```

Numerically:

```text
0.020699545503
= 0.002203822410
+ 0.018495723092.
```

Combining this with the existing baseline identity gives:

```text
baseline_gap
= finite_BU_closure
+ nonlinear_closure_gain
+ Delta_tangent.
```

Numerically:

```text
0.029768274718
= 0.009068729215
+ 0.018495723092
+ 0.002203822410.
```

This is a complete response budget.

It says that the initial ONA opening is partitioned into:

1. Closure already achieved by finite BU amplitude.
2. Additional nonlinear gain visible in the endpoint susceptibility.
3. A small irreducible local opening remaining at the physical point.

The most physically significant result is that CGM simultaneously has:

- A substantial integrated aperture of 2.07 percent, preserving historical reconstructibility.
- A much smaller differential aperture of 0.22 percent, giving near-perfect local response.

That is an effective combination of memory and stability:

> **The system remains globally open enough to preserve ancestry while being locally close enough to respond almost without loss.**

This may be the deeper physical reason why the aperture cannot be represented by a single number in every regime. `Delta_secant` controls integrated history. `Delta_tangent` controls local perturbations.

## 9. ONA and BU now have distinguishable mechanical roles

The elasticities are:

```text
elasticity_theta = 1.612965279633
elasticity_m     = 1.018886668547.
```

Thus:

- ONA controls the curvature sensitivity.
- BU controls the balance amplitude nearly linearly.

A one-percent change in ONA produces about a 1.613-percent change in `delta_BU`.

A one-percent change in BU amplitude produces about a 1.019-percent change.

This clarifies their physical division:

```text
ONA determines how strongly displacement bends ancestry.
BU determines how much balanced motion is available to carry it.
```

The ONA parameter is not simply another speed. It acts as the geometry-control variable. BU is the amplitude-control variable.

In continuum language, ONA behaves more like a curvature or steering susceptibility, while BU behaves more like a field amplitude.

## 10. The canonical spectrum has been decoded, but not completed

The six canonical bins up to length five are no longer mysterious:

| Canonical value | Generator origin |
|---|---|
| 0 | exact out-back cancellation |
| 0.166827788338 | UB dual pole |
| 0.195342178258 | OB dual pole |
| 0.420475081676 | UO–OB–UB triangle |
| 0.412719054050 | UO–OB–I–UB four-cycle |
| 0.256712834405 | crossed UB–OB–OB–UB four-cycle |

The previously unknown L4 value is now identified as the crossed dual-pole word:

```text
UNA -> BU+ -> ONA -> BU- -> UNA.
```

That is a real discovery. Every short canonical loop has become a word in the three elementary precession generators.

But the bounded census is not yet a complete spectrum theorem.

The generators have nontrivial angles, so longer words may generate:

- Additional conjugacy angles
- Dense subsets of SO(3)
- Recurrent near-identities
- Special invariant subgroups
- Further exact word equivalences

The statement that the canonical spectrum has six values is valid only for the reported bound `L <= 5`.

Completion requires a word-growth analysis at arbitrary or substantially larger depth.

## 11. The laboratory results reveal the noncompact sector

The lab results should not be dismissed as mere protocol artifacts. The latest closure census reveals something physical:

```text
closed lab words = 4 / 360
```

and all four are collinear BU pole out-backs with zero rotation.

Every bent named word retains a nonzero net boost.

Therefore the lab construction is not usually an SO(3) holonomy. It is a full Lorentz transformation containing both:

- Velocity drift
- Frame rotation

This is the noncompact complement of the canonical closed holonomy.

The canonical connection asks:

> What rotational ancestry remains after the path closes on the mass shell?

The lab word asks:

> What total boost and rotation are produced when the same relative displacements are successively issued in one inertial frame?

Those are different physical experiments.

The lab result is therefore a **roto-boost memory**. It expresses the inseparability of orientation change and momentum drift under open boost composition.

This is why its spectrum is much finer:

```text
canonical angle classes: 6
lab angle values:       147
```

Canonical transport quotients out the open velocity drift and retains intrinsic curvature. Lab composition retains the detailed operational history of how the boosts were issued.

A general Lorentz word is richer than the extracted `theta_lab`. Its physical content includes a rapidity, a rotation, their relative axes, and Lorentz conjugacy invariants. The analysis currently keeps only part of that information.

## 12. The spherical chart residual has found its proper meaning

The complete Cartesian Palge-Pfeifer connection converges to `delta_BU` for both BU and the palindrome:

```text
chart complete = geodesic = origin gyr = defect.
```

This closes the earlier ambiguity.

The spherical results are not new precession angles. But their phenomenon is still physical: they demonstrate the impossibility of describing global frame transport with one nonsingular spherical frame.

The poles and rest point force patch changes. The residuals expose missing frame transitions. This is analogous to gauge potentials that depend on a chart although their complete holonomy is invariant.

The physical phenomenon is the necessity of patching local descriptions of orientation. The individual values `G_z` and `G_diag` are not invariant, but the obstruction to one global regular chart is real.

The result

```text
G_diag - G_z ≈ 1
```

should remain a numerical implementation diagnostic unless an exact transition function is derived. It should not be promoted to a physical constant.

## 13. The compact-hyperbolic residual may be a spin-transport slip

The comparison

```text
phi_SU2 = 0.587900762654
3 delta_BU = 0.586026534773
residual = 0.001874227881
sigma_compact = residual / m_a
              = 0.009395985199
```

compares two different but related physical geometries:

- `phi_SU2` is compact rotational-fiber commutator memory.
- `delta_BU` is hyperbolic mass-shell transport memory.

The near lock suggests that three balance holonomies almost reconstruct the compact SU(2) commutator.

The residual can therefore be interpreted as a candidate **compact-hyperbolic slip**:

> The amount by which internal spin-frame closure and external momentum-space closure fail to coincide.

That could become the bridge between:

- Intrinsic spin
- Spin-orbit coupling
- Momentum transport
- Anomalous precession
- Compact gauge phase and spacetime curvature

But the present comparison is scalar. Before assigning it to a measured coupling, the group elements must be compared in a common SU(2) representation.

The breakthrough is not yet “sigma equals a known particle ratio.” It is that the work has isolated the precise quantity which would measure internal-versus-external transport mismatch if the matrix bridge closes.

## 14. The greatest implication for gravity

The current gravity kernel reduces a plaquette defect to

```text
alpha(d) = popcount(d) delta_BU / 6.
```

This keeps curvature magnitude but discards:

- Rotation axis
- Orientation sign
- Word order
- Noncommutative composition
- Compact family phase

The precession analysis has now proved that these discarded data are physically consequential.

Two loops may have:

- The same angle and different axes.
- The same endpoint and different word histories.
- Different composite angles despite containing the same elementary magnitudes.
- Different lab boosts even when their canonical conjugacy class agrees.

Therefore the scalar Regge defect is sufficient only for a highly symmetric sector, such as static spherical gravity where directional information averages out.

It cannot by itself be the final kernel for:

- Frame dragging
- Spin precession
- Gravitomagnetism
- Generic gravitational waves
- Rotating sources
- Non-spherical dynamical geometry
- Transport of polarization

The precession triad reveals the missing layer:

> **Gravity must ultimately use an oriented, matrix-valued holonomy, not only a scalar popcount deficit.**

This does not invalidate the scalar gravity derivation. It identifies its domain. It is the radial or trace-reduced projection of a richer connection.

## 15. Why the 32-bit lift is probably required for dynamical gravity

The six-bit defect

```text
d = q(x) XOR q(y)
```

records which transport modes differ, and `popcount(d)` records their magnitude shell.

But XOR is symmetric:

```text
q(x) XOR q(y) = q(y) XOR q(x).
```

It cannot by itself distinguish the orientation of the loop.

Precession changes sign when loop orientation is reversed. The axis and direction therefore require information beyond the six-bit shadow.

The missing orientation naturally resides in:

- Byte order
- Family phase
- Spinorial provenance
- The 32-bit lift that separates family phases collapsed in the 24-bit carrier

This provides a new explanation for the representation-boundary results elsewhere in the corpus:

> **The 24-bit shadow can support scalar curvature magnitude. The 32-bit spinorial lift is required to carry oriented curvature and hence complete precession dynamics.**

That connection is important. The 32-bit lift is not only needed to repair an electroweak polynomial or color bracket. It appears necessary for the physical sign and axis of curvature.

Static gravity can survive projection to the shadow because spherical averaging removes orientation. Dynamical gravity cannot.

## 16. Universality in its strongest form

The universal CGM precession principle can now be stated as follows:

> Whenever distinguishable operations compose noncommutatively but their result must remain traceable to a common origin, closed displacement returns as orientation.

This principle has different representations:

| Representation | Physical phenomenon |
|---|---|
| Einstein velocity ball | Thomas-Wigner rotation |
| Spin bundle | SU(2) geometric phase |
| Classical carried frame | Gyroscopic precession |
| Curved spacetime | Geodetic and frame-dragging precession |
| Orbital geometry | Apsidal precession |
| Gauge bundle | Wilson-loop phase |
| Finite hQVM | Holonomic pole exchange and family-phase memory |
| Gravity kernel | Regge curvature deficit |

The phenomena are not identical numerical angles. They are the same ancestry-preserving mechanism evaluated in different representations and at different scales.

That is the correct sense in which precession is universal.

## 17. What is already complete

Within the continuous stage realization, the following is now effectively closed:

1. The three elementary precession generators are identified.
2. Their axes are mutually orthogonal.
3. Their angles equal exact gyrotriangle defects.
4. The BU and UNA dual-pole loops are exact doubled elementary channels.
5. The palindrome is exact conjugation.
6. Magnitude preservation and axis transport are separated.
7. The complete spin connection reproduces the canonical result.
8. The lab construction is classified as an open Lorentz word.
9. The aperture has secant, tangent, and infinitesimal response meanings.
10. The short canonical spectrum has been reduced to generator words.
11. The scalar gravity chain is confirmed as a downstream projection of the OB balance channel.

That is already a substantial mechanics, not merely a numerical investigation.

# What remains for completion

## 18. Prove the universal precession theorem

The theoretical document needs a formal theorem with the following chain:

```text
CS gives orientation.
UNA makes operation order physically relevant.
ONA keeps different paths comparable.
BU closes the observable endpoint.
A closed noncommutative displacement must leave an element
of the endpoint stabilizer.
For the derived three-dimensional continuous realization,
that stabilizer is SO(3), with spinorial lift SU(2).
Therefore the retained ancestry is precession.
```

This theorem would connect the modal conditions directly to the physical phenomenon. At present the ingredients are distributed across the logic, 3D, Hilbert, holonomy, and precession documents.

## 19. Add group-valued reporting

Angles alone are insufficient.

For every canonical and lab word, record:

```text
rotation matrix
unit quaternion
axis
signed angle
SU(2) lift
trace in spin-j representations
word orientation
```

For canonical words, the core object should be the quaternion or SU(2) matrix, not the unsigned angle.

The angle catalog should become a projection of the group-valued catalog.

### Required code fields

```python
{
    "path": path,
    "R_so3": R,
    "q_su2": quaternion,
    "axis": axis,
    "theta_signed": theta,
    "trace_so3": trace_R,
    "trace_su2": 2 * cos(theta / 2),
    "phase_plus": +theta / 2,
    "phase_minus": -theta / 2,
    "relative_phase": theta,
}
```

## 20. Add the two missing closure identities

Add exact gates:

```python
Delta_secant
= Delta_tangent + nonlinear_closure_gain
```

and

```python
baseline_gap
= finite_BU_closure
+ nonlinear_closure_gain
+ Delta_tangent.
```

Also report the geometric action:

```python
S_relative_over_hbar = theta
S_branch_over_hbar = theta / 2
```

for every elementary and composite canonical holonomy.

This moves the physical mapping beyond equivalent circular speeds and into direct quantum phase.

## 21. Complete the canonical word algebra

Do not extend the current brute-force enumeration directly. Use unit quaternions and dynamic programming.

For each depth:

1. Store `(last_stage, canonical_quaternion)`.
2. Identify `q` and `-q`.
3. Cluster only within a declared high-precision tolerance.
4. Track exact predecessor words.
5. Count unique group elements, unique conjugacy angles, and axes.
6. Search for recurrence near identity.
7. Test whether the generated subgroup is finite, discrete infinite, or dense.

The result should report:

```text
N_group(L)
N_conjugacy(L)
minimum nonzero angle
nearest return to identity
axis coverage on S2
word entropy
```

The six-bin result must remain explicitly labeled `L <= 5`.

## 22. Complete the lab Lorentz classification

For each lab word, retain the full Lorentz matrix.

Compute:

```text
net rapidity
net boost direction
rotation angle
rotation axis
boost-rotation axis alignment
trace Lambda
trace Lambda^2
eigenvalue structure
Lorentz conjugacy class
```

The extracted rotation is only one component of the phenomenon.

The named BU and palindrome lab words should be described as open roto-boosts with simultaneous velocity and orientation memory.

The scalar

```text
theta_lab - theta_can
```

should remain secondary. The primary comparison should be the relative group element between the complete canonical transport and the complete lab Lorentz transport.

## 23. Construct the compact-hyperbolic bridge at matrix level

The next decisive computation is not another numerical fit.

Construct:

```text
U_compact = SU2 commutator
U_BU = SU2 lift of canonical BU holonomy
U_relative = U_compact inverse times the correctly aligned product of U_BU factors
```

Because axes matter, test all geometrically dictated conjugations before multiplying three BU elements.

Report:

```text
trace(U_relative)
axis(U_relative)
angle(U_relative)
whether sigma_compact is its actual conjugacy angle
```

If the residual survives as a group invariant, it becomes a genuine physical spin-transport slip.

If it disappears after correct conjugation, the present scalar residual was comparing misaligned rotations.

Either outcome is conclusive.

## 24. Lift scalar kernel curvature to oriented curvature

The next gravity implementation should preserve the ordered plaquette:

```text
K(x,y) = T_x T_y T_x^-1 T_y^-1
```

rather than immediately reducing it to `popcount(q(x) XOR q(y))`.

For each ordered byte pair, retain:

```text
transport defect d
reverse orientation sign
family phase
commutator permutation
32-bit lifted signature
shell
candidate SU(2) or SO(3) holonomy
```

Then derive an equivariant map from the full ordered finite signature to the precession triad.

The required conditions are:

```text
K(y,x) = K(x,y)^-1
identity defect maps to identity rotation
family phase determines spinorial sheet
axis map respects permutation of the three spatial axes
continuous stage anchors reproduce UO, OB, and UB
```

Only after those gates pass should the matrix-valued Regge sum be introduced.

## 25. Extend gravity from scalar depth to oriented transport

The current scalar result should be retained as the static spherical projection.

The oriented extension should separately compute:

```text
scalar curvature magnitude
vector precession axis
STF tensor projection
holonomy composition over plaquette surfaces
```

The first required observables are:

1. Gyroscope transport around a static spherical source.
2. Frame dragging around a rotating source.
3. Polarization transport along null paths.
4. Gravitational-wave parallel transport.
5. Recovery of the existing scalar `tau_G` after spherical averaging.

This is where the UO and UB channels should acquire definitive gravitational roles.

The expected structure to test is:

```text
OB channel -> radial gravitoelectric balance
UO channel -> transport and orbital-axis steering
UB channel -> rotational balance / gravitomagnetic sector
```

This is a derivation target, not a free assignment.

## 26. Prove the Einstein-ball realization

The largest remaining foundational dependency is the map

```text
u_p, o_p, m_a -> Einstein beta values.
```

The program currently declares this as an extra realization identification.

Completion requires showing that the map is forced by:

- Lorentz invariance
- Bounded reachability
- Common-source identity
- Gyroassociative composition
- Smoothness
- Correct nonrelativistic limit
- BU dual-pole symmetry

The derivation must exclude plausible alternatives such as interpreting the thresholds as rapidities, half-rapidities, ordinary angles, or nonlinear functions of velocity.

Until that bridge is proved, the precession mechanics is exact within the chosen Einstein-ball realization but not yet uniquely forced from the modal layer.

## 27. Build the representation ladder

For every canonical holonomy `H`, compute its action in:

```text
scalar representation
vector SO(3) representation
spinor SU(2) representation
spin-1 representation
rank-2 STF representation
finite hQVM family representation
```

This will show exactly how one universal precession phenomenon becomes:

- Spin phase
- Vector-axis rotation
- Quadrupole transport
- Gravitational STF curvature
- Finite carrier memory

The representation ladder is the missing formal bridge between “one universal mechanism” and “many physical phenomena.”

## Final assessment

The latest run has found something substantially deeper than a precession angle.

It has found a **universal three-channel conversion law between motion and memory**:

```text
rotation + translation -> axis steering
translation + balance  -> curvature
balance + rotation     -> intrinsic rotational stabilization
```

The dual-pole loop selects the translation-balance channel as gravitational curvature. The palindrome proves that the curvature magnitude can remain identical while its orientation carries additional history. The closure derivatives show that the aperture is a nonlinear response law with distinct global and local openings. The complete spin connection proves that the same result is physical mass-shell holonomy, not a coordinate artifact.

The most important next step is not to search for more numerical coincidences. It is to preserve the full group element through every formal layer.

The scalar angle gave the aperture and static gravity. The **axis, sign, ordering, and spinorial sheet** are what should now produce dynamical gravity, spin transport, and the complete physical meaning of precession.